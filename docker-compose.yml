version: '3.8'

services:
  recsalmo:
    build: .
    container_name: recsalmoContainer
    volumes:
      - ./input_folder:/input
      - ./output_folder:/output
    command: ["--input", "/input", "--output", "/output"]
    ports:
      - "8080:8080"