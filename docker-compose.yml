version: '3.8'

services:
  recsalmo:
    build: .
    container_name: recsalmoContainer
    volumes:
      - ./input:/input
      - ./output:/output
    command: ["--input", "/input", "--output", "/output"]
    ports:
      - "8080:8080"